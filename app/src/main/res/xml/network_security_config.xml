<?xml version="1.0" encoding="utf-8"?>
<!--
    Network Security Configuration for HustleHub.

    Production: cleartext blocked by default.
    Development & Local Testing: cleartext permitted for local IP addresses and emulator loopbacks.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Permit cleartext HTTP for local backend testing (emulator, Wi-Fi dev IPs, localhost) -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.238.26.212</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <domain includeSubdomains="true">localhost</domain>
    </domain-config>

    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
