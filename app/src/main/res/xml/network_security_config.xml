<?xml version="1.0" encoding="utf-8"?>
<!--
    Network Security Configuration for HustleHub.

    Production: only system CAs trusted, cleartext blocked.
    Debug:      also trusts user-installed CAs (for Charles Proxy / mitmproxy).
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
